# ADR 0003 — Environment variables management

- **Status**: Accepted
- **Decision**: Support `MONGO_URI` and legacy keys; `.env` per environment; never commit secrets.
- **Consequences**: Easier rotation and clarity.
